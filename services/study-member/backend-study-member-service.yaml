apiVersion: v1
kind: Service
metadata:
  name: backend-study-member-service
spec:
  selector:
    app: backend-study-member-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP